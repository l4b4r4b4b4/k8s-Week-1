apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers:
    - name: client
      image: lukeskywalker88/multi-client
      ports:
        - containerPort: 3000